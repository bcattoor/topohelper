namespace TopoHelper.Model.Enums
{
    /// <summary>
    /// The direction of the cant depends on the direction of the track, when a
    /// left rail is raised it is CW, else it is CCW.
    /// </summary>
    internal enum CantDirection
    {
        Clockwise, CounterClockwise
    }
}